fn main() {
    let num = vec![1,2,3];
    let num_iter = num.iter();
    for val in num_iter {
        println!("{}",val); //cant chane immutable reference these are 
    }
} //to mut we can use a mutable iterator .iter_mut
fn main() {
    let x = 5;

    let mut num = vec!  [1,2,3,4,5];
    let mut nu_iter = num.iter_mut;
    while Some(val) = v1_iter.next() {
        printl!("{}",val)
    }

} 