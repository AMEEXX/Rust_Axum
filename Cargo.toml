[package]
name = "Rust"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.41"
futures = "0.3"
tokio = { version = "1", features = ["full"] }
serde = {version = "1", features = ["derive"]}
serde_json = "1"
axum = "0.7"
tower-http = {version = "0.6", features = ["fs"]}
